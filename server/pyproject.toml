[project]
name = "data-fetching"
version = "0.1.0"
description = "This part of the project manages the communication with the website."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "playwright>=1.58.0",
]
